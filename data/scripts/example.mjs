/**
 * Example keybind script.
 * Place `.mjs` files in the scripts folder and select them in the Keybinds tab.
 *
 * export default async function () { ... }
 * // or: export async function run() { ... }
 */
export default async function run() {
  console.log('[keybind] example.mjs ran')
}
